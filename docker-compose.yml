services:
  taskit:
    image: taskit/taskit:latest
    build: .
    container_name: taskit
    environment:
      TZ: ${TZ:-UTC}
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
